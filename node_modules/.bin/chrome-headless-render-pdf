#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../chrome-headless-render-pdf/dist/cli/chrome-headless-render-pdf.js" "$@"
  ret=$?
else 
  node  "$basedir/../chrome-headless-render-pdf/dist/cli/chrome-headless-render-pdf.js" "$@"
  ret=$?
fi
exit $ret
